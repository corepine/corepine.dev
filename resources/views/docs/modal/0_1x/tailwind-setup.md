# Tailwind Setup

Tailwind and Vite setup is now documented on the main onboarding page.

Use [Installation & Setup](doc:installation) for:

- CSS import through `resources/css/app.css`
- When to use `include-styles`
- Asset rebuild steps
- One-pipeline vs host-injected style strategy

Then continue with [Quick Start](doc:quick-start).
