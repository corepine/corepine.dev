# Setup

Setup is now folded into the main onboarding page.

Use [Installation & Setup](doc:installation) for:

- Package install
- Config publishing
- Modal host placement
- CSS strategy
- Asset rebuild steps

After that, continue with [Quick Start](doc:quick-start).
